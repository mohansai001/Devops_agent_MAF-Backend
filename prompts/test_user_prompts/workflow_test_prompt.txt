create an ci for the python fastapi and then terraform for it with dummy azure webapp resourcename which I will change manually and the cd for the terraform and return an dummy webapp link for us to see.
just create dummy files the tools have push capability in tfagent snd yamlagent so follow through.
        Details:
        - Repository: Hari-var/test_repo


        - Instructions:
        1. Create a new branch from main called "feature/add-readme" if it does not exist.
        2. Add the file at the specified path.
        3. Commit the file with the given message.
        4. Do not modify any other files.
        5. Use the appropriate tool to push the file to the repository.