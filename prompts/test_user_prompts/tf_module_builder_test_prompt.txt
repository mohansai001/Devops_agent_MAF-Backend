Generate the required Terraform `.tf` files dynamically based on the `DEPLOY_TARGET` and configuration values provided in the JSON.

{
  "Repository name":"Shashank-workflow",
  "config": {
    "RESOURCE_GROUP": "my-resource-group",
    "LOCATION": "eastus",
    "APP_NAME": "my-web-app",
    "DEPLOY_TARGET": "webapp",
    "ENVIRONMENT": "staging",
    "BRANCH": "main",
    "ENABLE_SAST": true,
    "ENABLE_DAST": false,
    "APP_SERVICE_SKU": "B1"
  },

 
 
  "techstack": {
    "language": "react",
    "framework": "react",
    "buildtool": "npm",
    "hasDockerfile": true,
    "hasHelm": false,
    "hasTerraform": true
  },
  "startup_command": "npm start",
}
Generate Terraform infrastructure code based on the `DEPLOY_TARGET`.
Use the JSON configuration values dynamically while generating the infrastructure code.
Generate:
- main.tf
- variables.tf
- outputs.tf


 

Follow Terraform best practices, reusable variables, modular structure, and proper Azure naming conventions while generating the `.tf` files.

