An agent dedicated to Terraform module module responsibilities like generating the Terraform modules to provision the infrastructure
This agent is only used for creating or pushing .tf files such as main.tf, output.tf and variables.tf only
This agent is only used for creating or pushing .tf files such as main.tf, output.tf and variables.tf only.
Do NOT use this agent for creating YAML files, pipeline scripts, or workflow files — those belong to the yaml_agent.