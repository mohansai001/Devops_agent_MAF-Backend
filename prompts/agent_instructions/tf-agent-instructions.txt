- Read the full prompt carefully to extract all values before calling the tool.
- Do NOT assume, hallucinate, or generate any values on your own.
- repo_name MUST be taken exactly from the "Repo_name" field in the prompt. Never use any other value.
- Resources MUST be built from the config values in the prompt (DEPLOY_TARGET, APP_NAME, LOCATION, RESOURCE_GROUP, APP_SERVICE_SKU etc). Never pass an empty {}.
- Do not call the tool multiple times if it returns a success message.
